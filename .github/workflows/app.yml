name: app

on:
  push:
    branches: [main]
  workflow_call:

permissions:
  contents: read

jobs:
  build:
    name: Build app
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7
        with:
          persist-credentials: false

      - name: Set up environment
        uses: ./.github/workflows/setup
        with:
          filters: app...

      - name: Build app
        env:
          CLOUDFLARE_ENV: preview
        run: pnpm -F app run build

      - name: Upload app artifact
        uses: actions/upload-artifact@v7
        with:
          name: app-build
          path: |
            app/dist
            app/.wrangler/deploy/config.json
          retention-days: 7
          include-hidden-files: true

  build-nextjs:
    name: Build Next.js app
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7
        with:
          persist-credentials: false

      - name: Set up environment
        uses: ./.github/workflows/setup
        with:
          filters: app...

      - name: Type check Next.js app
        run: pnpm -F nextjs run tsc

      - name: Build Next.js app
        run: pnpm -F nextjs run build-worker

      - name: Upload Next.js artifact
        uses: actions/upload-artifact@v7
        with:
          name: nextjs-dist
          path: nextjs/.open-next
          retention-days: 7
          include-hidden-files: true

  test:
    name: Test ${{ matrix.name }}
    needs: [build, build-nextjs]
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            name: Chrome
            engine: chromium
            project: chrome
            test: xvfb-run pnpm -F app run test
          - os: ubuntu-latest
            name: Firefox
            engine: firefox
            project: firefox
            test: xvfb-run pnpm -F app run test
          - os: ubuntu-latest
            name: Android
            engine: chromium
            project: android
            test: xvfb-run pnpm -F app run test
          - os: macos-latest
            name: Safari
            engine: webkit
            project: safari
            test: pnpm -F app run test
          - os: macos-latest
            name: iOS
            engine: webkit
            project: ios
            test: pnpm -F app run test
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7
        with:
          persist-credentials: false

      - name: Set up environment
        uses: ./.github/workflows/setup
        with:
          filters: app...

      - name: Install Playwright
        uses: ./.github/workflows/install-playwright
        with:
          engine: ${{ matrix.engine }}
          workspace: app

      - name: Download app artifact
        uses: actions/download-artifact@v8
        with:
          name: app-build
          path: app

      - name: Download Next.js artifact
        uses: actions/download-artifact@v8
        with:
          name: nextjs-dist
          path: nextjs/.open-next

      - name: Test
        id: test
        run: ${{ matrix.test }} --project ${{ matrix.project }} --pass-with-no-tests --grep-invert @visual

      - name: Upload test results to GitHub
        if: failure() && steps.test.outcome == 'failure'
        uses: actions/upload-artifact@v7
        with:
          name: app-${{ matrix.project }}-test-results
          path: app/test-results
          retention-days: 7

  visual:
    name: ${{ matrix.name }}
    needs: [build, build-nextjs]
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            name: Visual
            engine: chromium
            secondary_engine: firefox
            project: chrome
            secondary_project: firefox
            projects: chrome firefox
            test: xvfb-run pnpm -F app run test-visual
            results: visual
          - os: macos-latest
            name: Visual (Safari)
            engine: webkit
            secondary_engine: ""
            project: safari
            secondary_project: ""
            projects: safari
            test: pnpm -F app run test-visual
            results: visual-safari
    env:
      VISUAL_PROJECTS: ${{ matrix.projects }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7
        with:
          persist-credentials: false

      - name: Set up environment
        uses: ./.github/workflows/setup
        with:
          filters: app...

      - name: Install Playwright
        uses: ./.github/workflows/install-playwright
        with:
          engine: ${{ matrix.engine }}
          workspace: app

      - name: Install secondary Playwright engine
        if: matrix.secondary_engine != ''
        uses: ./.github/workflows/install-playwright
        with:
          engine: ${{ matrix.secondary_engine }}
          workspace: app

      - name: Download app artifact
        uses: actions/download-artifact@v8
        with:
          name: app-build
          path: app

      - name: Download Next.js artifact
        uses: actions/download-artifact@v8
        with:
          name: nextjs-dist
          path: nextjs/.open-next

      - name: Test visual
        id: test-visual
        # -x stops after the first failing test so we can rerun once with -u below.
        run: |
          projects=()
          for project in $VISUAL_PROJECTS; do
            projects+=(--project "$project")
          done
          ${{ matrix.test }} "${projects[@]}" --grep @visual --pass-with-no-tests -x

      - name: Update screenshots
        id: update-screenshots
        if: failure() && steps.test-visual.outcome == 'failure'
        # Record the pre-update timestamp so we can delete stale screenshots
        # that are no longer regenerated by this rerun.
        run: |
          touch -t "$(date -u -d '-1 second' '+%Y%m%d%H%M.%S' 2>/dev/null || date -u -v-1S '+%Y%m%d%H%M.%S')" "${{ runner.temp }}/pre-update-marker"
          projects=()
          for project in $VISUAL_PROJECTS; do
            projects+=(--project "$project")
          done
          ${{ matrix.test }} "${projects[@]}" --grep @visual -u

      - name: Remove stale screenshots
        if: always() && steps.update-screenshots.outcome == 'success'
        # The update run only touches files that still exist, so remove any
        # older screenshots for these browsers that were left behind.
        run: |
          for project in $VISUAL_PROJECTS; do
            find app/src -path "*/__screenshots__/*-${project}.png" ! -newer "${{ runner.temp }}/pre-update-marker" -delete
          done

      - name: Pack screenshots
        if: >
          always() &&
          (steps.test-visual.outcome == 'success' ||
          steps.update-screenshots.outcome == 'success')
        # The follow-up job applies screenshots from artifacts instead of
        # mutating the branch directly from each matrix shard.
        run: |
          mkdir -p "${{ runner.temp }}/artifacts"
          python <<'PY'
          import tarfile
          from pathlib import Path

          projects = "${{ matrix.projects }}".split()
          root = Path("app/src")
          for project in projects:
              archive_path = Path("${{ runner.temp }}/artifacts") / f"app-visual-{project}.tar.gz"
              screenshots = sorted(root.glob(f"**/__screenshots__/*-{project}.png"))
              if not screenshots:
                  continue
              with tarfile.open(archive_path, "w:gz") as tar:
                  for screenshot in screenshots:
                      tar.add(screenshot, arcname=screenshot.as_posix())
          PY

      - name: Upload screenshots (${{ matrix.project }})
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: app-visual-${{ matrix.project }}
          path: ${{ runner.temp }}/artifacts/app-visual-${{ matrix.project }}.tar.gz
          if-no-files-found: ignore
          retention-days: 1

      - name: Upload screenshots (${{ matrix.secondary_project }})
        if: always() && matrix.secondary_project != ''
        uses: actions/upload-artifact@v7
        with:
          name: app-visual-${{ matrix.secondary_project }}
          path: ${{ runner.temp }}/artifacts/app-visual-${{ matrix.secondary_project }}.tar.gz
          if-no-files-found: ignore
          retention-days: 1

      - name: Upload test results to GitHub
        if: failure() && (steps.test-visual.outcome == 'failure' || steps.update-screenshots.outcome == 'failure')
        uses: actions/upload-artifact@v7
        with:
          name: app-${{ matrix.results }}-test-results
          path: app/test-results
          retention-days: 7

  visual-finalize:
    # This job intentionally assumes PRs come from branches on this repository.
    # Fork-safe write-back logic was removed with the member-only PR model.
    name: Update screenshots
    needs: [test, visual]
    if: always() && needs.test.result != 'cancelled' && needs.visual.result != 'cancelled'
    permissions:
      actions: read
      contents: read
    uses: ./.github/workflows/visual.yml
    with:
      event: ${{ github.event_name }}
      visual-result: ${{ needs.visual.result }}
    secrets: inherit

  deploy-preview:
    # Preview deployments and commit statuses intentionally run only under the
    # same-repo PR model adopted by this repository.
    name: Preview
    needs: [build, build-nextjs]
    if: always()
    permissions:
      actions: read
      contents: read
      deployments: write
      statuses: write
    uses: ./.github/workflows/deploy-preview.yml
    secrets: inherit
