{
  "name": "reakit-theme-classic",
  "version": "0.0.1",
  "description": "Reakit classic theme",
  "license": "MIT",
  "repository": "reakit/reakit",
  "main": "lib/index.js",
  "module": "es/index.js",
  "unpkg": "dist/reakit-theme-classic.min.js",
  "types": "ts",
  "author": {
    "name": "Diego Haz",
    "email": "hazdiego@gmail.com",
    "url": "https://github.com/diegohaz"
  },
  "scripts": {
    "test": "jest",
    "coverage": "yarn test --coverage",
    "postcoverage": "opn coverage/lcov-report/index.html",
    "type-check": "tsc --noEmit",
    "lint": "eslint . --ext js,ts,tsx",
    "build:clean": "rimraf dist es lib ts && node scripts/cleanProxies",
    "build:ts": "tsc --emitDeclarationOnly",
    "prebuild": "yarn build:clean && yarn build:ts",
    "build": "rollup -c && node scripts/makeProxies",
    "dev": "echo 'TODO'",
    "preversion": "yarn lint && yarn test && yarn build",
    "postpublish": "yarn build:clean"
  },
  "keywords": [
    "reakit",
    "react",
    "theme"
  ],
  "peerDependencies": {
    "reakit": "^1.0.0-alpha.0"
  }
}
