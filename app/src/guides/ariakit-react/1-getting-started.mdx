---
title: Getting Started
description: Ariakit React is an open-source library offering unstyled primitive components to build accessible web apps, design systems, and component libraries.
---

import Sample from "#app/components/sample.astro";
import { source as s0 } from "#app/examples/combobox-group/preview.astro";

## Installation

1. First, make sure you have `react` and `react-dom` installed. Ariakit React works with React 18 and above.

2. Install Ariakit React.

   ```sh
   npm install @ariakit/react
   ```

## Usage

<Sample source={s0} />
