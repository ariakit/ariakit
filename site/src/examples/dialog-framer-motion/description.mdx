import ContentLink from "#app/components/content-link.astro";

Using [Framer Motion](https://motion.dev) to add initial and exit animations to a modal <ContentLink href={`/${props.framework}/components/dialog/`}>Dialog</ContentLink> and its `backdrop` element.
