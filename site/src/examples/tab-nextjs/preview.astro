---
/**
 * Preview component for the Next.js Tab example.
 * This doesn't render a component directly - the preview is shown via iframe
 * pointing to the Next.js app.
 */
import sourceReact from "nextjs/app/tab-nextjs/page.tsx?source";

export const source = {
  react: sourceReact,
};
---

<!-- Next.js examples are previewed via iframe, no component rendered here -->
