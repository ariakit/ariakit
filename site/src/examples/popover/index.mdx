---
title: Popover
frameworks:
  - react
---

# {frontmatter.title}

Description {props.framework}

import CodeBlock from "../../components/code-block.astro";
import CodeBlockTabs from "../../components/code-block-tabs.astro";
import data from "./index.react.tsx?deps-raw";

<CodeBlockTabs
  aiPrompt="Add a new tab with the code for the popover component"
  preview
  tabs={Object.entries(data.files).map(([filename, code]) => ({
    filename,
    code,
  }))}
/>
