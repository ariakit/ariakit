---
"@ariakit/react-core": patch
"@ariakit/react": patch
---

Fixed [`Menu`](https://ariakit.org/reference/menu) with `modal` and persistent elements so focusing a persistent [`MenuButton`](https://ariakit.org/reference/menu-button) doesn't immediately move focus back to the menu.
