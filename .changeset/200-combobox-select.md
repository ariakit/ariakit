---
"@ariakit/components": patch
"@ariakit/react-components": patch
"@ariakit/react": patch
---

Added `ComboboxSelect` for rendering a select-like combobox disclosure backed by the existing combobox selected value, and exposed `labelElement` and `selectElement` on the combobox store for select-like combobox integrations.
